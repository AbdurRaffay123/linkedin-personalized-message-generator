# Copy to .env.local. Points the dashboard at the backend API.
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
# API key for the hardened backend (python -m app.issue_key you@example.com):
NEXT_PUBLIC_API_KEY=
